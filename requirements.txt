# 基本依赖
beautifulsoup4>=4.12.0
curl-cffi>=0.5.0
asyncio

# 数据库支持
# SQLite - 内置，无需额外安装

# PostgreSQL支持 (可选)
psycopg2-binary>=2.9.0

# Supabase支持 (可选)
supabase>=1.0.0

# 异步支持 (可选，用于更高性能)
httpx>=0.24.0
aiofiles>=23.0.0

# 日志和工具
pathlib2; python_version < "3.4"

python-dotenv
feedparser
fast-readability
html5lib
readability-lxml

pytz